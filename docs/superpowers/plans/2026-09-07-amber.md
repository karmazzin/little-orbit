# Amber Implementation Plan

**Goal:** Playable autumn planet and persistent grove → Lev → amber → nearby frame travel loop.
**Architecture:** Separate runtime entry routes ?planet=amber to Amber; default loads existing main. Pure travel state, injectable movement terrain, deterministic spherical Amber geography/view.
**Tech Stack:** TypeScript, Three.js, node:test, existing Vite.
**Spec:** docs/superpowers/specs/2026-09-07-amber-design.md

## Tasks
- [x] 1. Build deterministic Amber geography/view in src/amber-world.ts and tests/amber-world.test.ts. Export AMBER_RADIUS=51.2, AMBER_PLACES ({id,name,text,up,kind}), AMBER_SPAWN, amberSample compatible TerrainSample, buildAmberWorld(scene) returning obstacles, residents ({id,name,role,text,up,model}), update(time,dt), treeCount. Connect all sites with walkable trails, 2–3 lakes and small streams; density and coverage tests.
- [x] 2. Add optional movement environment to simulation createPlayer/step/movementAction, preserving defaults and regression tests. Create src/travel.ts pure restore/reducer and frame placement; tests cover locked/repeat/persistence guards. Khvoya main integrates Lev conditional dialogue, frame E interaction and journal item. Reset includes new keys. Rename crater text.
- [x] 3. src/entry.ts selects runtime; src/amber-main.ts implements walking/swimming, camera, keyboard and touch, dialogue, discovered journal, global overview, save/reload and return. Existing main startup unchanged on default route. Persist isolated Amber player and exploration; never clear Khvoya save.
- [x] 4. Run relevant tests/full suite/build; independent review of integration and world; browser inspect Khvoya dialogue gate and Amber surface, route, return and reload. Document result in README.

## Decisions and progress
Ruling: User explicitly requested implementation in this running game, so current clean checkout is used; no repeated approval gate or deployment. Agent owns only Amber geometry; parent owns integration, movement and state.
Ruling: Initial local NPCs have simple walks and greetings; no new prerequisite quest is added to unlock.

User correction: replace custom Amber UI with existing index.html IDs/style and shared HUD components. Agent amber_shared_ui owns runtime correction. Parent owns shared sky radius/state injection, Amber neighbor ephemerides and system-map current-world flag. First whole suite passed 184 tests before this correction; rerun after final UI/sky integration.
Task 1: complete — deterministic winding trails/streams, dry bridges, distributed sites, moving residents, tests.
Task 2: complete — critical save failures block travel; task reviewer approved fix.

Validation: full suite 187/187 passed (bounded test concurrency), production build passed, scoped review 15/15 tests and no blocking findings. Browser confirmed Lev grove topic and gift, frame activation/entry, shared Amber welcome/HUD, actual globe, current-system selection and saved village in World journal. Initial standalone return was checked; final shared-UI return replay could not finish due repeated approval-review timeouts. No workaround attempted. Latest fixes handle new concurrent World journal tab and persist arrival before removing URL marker. Temporary QA fixture removed; user's network server remains running.
